platform :ios, '17.0'

target 'Unearth' do
  use_frameworks!

  # 高德地图 SDK
  pod 'AMapFoundation'             # 高德基础 SDK
  pod 'AMap3DMap'                  # 高德 3D 地图 SDK (包含 MAMapKit)
  pod 'AMapLocation'               # 高德定位 SDK
  pod 'AMapSearch'                 # 高德搜索 SDK (路线规划)

end
